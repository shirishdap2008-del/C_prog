#include<stdio.h>
int main()
{
int m,n;
printf("enter array sizes m and n:");
scanf("%d %d",&m,&n);
int a[m][n];
int b=0;
printf("enter array values:");
for(int i=0;i<m;i++)
{
for(int j=0;j<n;j++)
{
scanf("%d",&a[i][j]);
b+=a[i][j];
}
printf("%d is the sum of row\n",b);
b=0;
}
return 0;
}
